<%= form_for(@trainer) do |f| %>
  <% if @trainer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trainer.errors.count, "error") %> prohibited this trainer from being saved:</h2>

      <ul>
      <% @trainer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  </br>
  <div class="field">
    <%= f.label :gender %> (male or female)<br>
    <%= f.text_field :gender, class:"text-center text-input", placeholder: "Male or Female" %>
  </div>
  </br>
  <div class="field">
    <%= f.label :weight %> (lbs)<br>
    <%= f.number_field :weight, step: 'any', class:"text-center text-input", placeholder: "Weight in Pounds" %>
  </div>
  </br>
  <div class="field" placeholder="height in inches">
    <%= f.label :height %> (inches)<br>
    <%= f.number_field :height, step: 'any',class:"text-center text-input", placeholder: "Height in Inches" %>
  </div>
  </br>
  <div class="actions">
    <%= f.submit 'Add Trainer', class:"btn btn-danger carnival-button" %>
  </div>
  </br>
<% end %>
